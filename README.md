# LCNAF-and-LCSH-JSON-activity-feed-crawlers
Crawls paginated JSON-LD activity stream feeds for LCNAF (lcnaf_crawler.py) and LCSH (lcsh.crawler.py) and downloads the object URLs for items whose "published" date is within the last N day.
